package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Dark Palette
val CharcoalBg = Color(0xFF0F0F11)
val SlateCard = Color(0xFF191A20)
val DarkSteel = Color(0xFF2C2F3A)

// Core Brand Tones (Copper & Metal Sparks / Wood Finish)
val GlowingCopper = Color(0xFFF28F3B) // Accent primary
val WarmGold = Color(0xFFFFD460) // High-contrast secondary
val AmberSpark = Color(0xFFE55822)

// Category Colors & Indicators
val FurnCategory = Color(0xFF3A86FF) // Electric Blue for Furniture
val MechCategory = Color(0xFF06D6A0) // Emerald for Mechanical
val SpamCategory = Color(0xFFEF476F) // Coral for Spam/Irrelevant

// Neutral Texts
val TextLight = Color(0xFFF3F4F6)
val TextGray = Color(0xFF9CA3AF)
val TextDark = Color(0xFF111827)

// Light Palette (Fallback but keeping deep premium feel)
val LightBg = Color(0xFFF9FAFB)
val LightCard = Color(0xFFFFFFFF)
val PrimaryLight = Color(0xFFD96B27)
